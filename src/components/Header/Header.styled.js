import styled from "styled-components";

export const HeaderBlock = styled.div`
  display: block;
  padding: 10px 0;
  width: 100%;
  box-shadow: 1px 0 3px var(--color-dark-400);

  .logo {
    margin-right: auto;
  }
`;

export const HeaderNav = styled.nav`
  display: flex;
  align-items: center;
`;
